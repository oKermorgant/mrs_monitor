local_costmap:
  global_frame: $(arg robot)/odom
  robot_base_frame: $(arg robot)/base_link

  update_frequency: $(arg controller_frequency)
  publish_frequency: $(arg controller_frequency)
  transform_tolerance: 0.5  

  static_map: false
  rolling_window: true
  width: 3
  height: 3
  resolution: 0.05
  
